
"""
    sum = function_a(a, b)
    sum == a + b

Return the sum of `a` and `b`.

It can render math too

```math
x_{1,2} = \\frac{a}{b}
```

!!! warning
    This function is not optimized for speed.

!!! note
    This function is slow

!!! tip
    This function is not fast

!!! example
    ```julia
    julia> function_a(1, 2)
    3
    ```



"""
function function_a(a, b)
    return a + b
end